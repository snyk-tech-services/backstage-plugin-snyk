export interface OrgData {
        attributes: {
        group_id?: string,
        is_personal: boolean,
        name: string,
        slug: string
        }
        id: string,
        type: string
}